## Building and Running the C++ Example

To build the C++ example from the project root:

```bash
g++ -O3 -march=native -Iinclude examples/cpp/example_interp.cpp lib/libgridfit.a -o examples/cpp/example_interp
```

To run the example:

```bash
./examples/cpp/example_interp
```

